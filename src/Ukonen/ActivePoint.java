package Ukonen;

public class ActivePoint {
    Node top;
    int edge;
    int length;

    public ActivePoint(Node top, int edge, int length) {
        this.top = top;
        this.edge = edge;
        this.length = length;
    }
}
